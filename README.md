# 4부 찬양대 베이스 배치도

교회 성가대 좌석 배치(본좌석 66석 + 만석 초과 인원용 보조석)를 PC/모바일 브라우저에서 직접 편집·저장·이미지로 공유하는 단일 HTML 도구.

## 사용 방법

### 아이폰/모바일 (추천): 홈 화면에 추가

1. 인터넷 되는 곳에서 **Safari로** 배포 주소를 엽니다: **https://jayden-choo-aki.github.io/seating-chart/**
   - 카카오톡에서 링크를 열었다면 먼저 하단 메뉴(⋯)에서 **"Safari로 열기"**를 누르세요. 카톡 인앱 브라우저에서는 홈 화면 추가가 안 됩니다.
2. 화면 하단 가운데 **공유 버튼(⬆︎)** → 아래로 스크롤 → **"홈 화면에 추가"** → **"추가"**를 누릅니다.
3. 이후에는 홈 화면의 "베이스 배치도" 아이콘으로 실행 — **인터넷이 전혀 없는 곳에서도 동작**합니다.
   - 카카오톡으로 받은 HTML 파일의 미리보기(바로보기)로는 열리지 않습니다(안내 문구만 표시). 반드시 위 방법으로 설치하거나 링크로 접속해 주세요.
   - 온라인 상태에서 열면 최신 버전으로 자동 갱신됩니다.

### PC

1. `index.html`을 브라우저로 열기 (더블클릭). **인터넷 연결 불필요** — 이 파일 하나만 있으면 어디서든 동작합니다.
2. 단원 카드를 클릭(또는 드래그) → 빈 좌석을 클릭(또는 드롭).
3. 배치된 좌석을 클릭해 선택한 뒤 다른 좌석 클릭 → 빈 좌석이면 이동, 배치된 좌석이면 서로 교체.
4. 점유 좌석 더블클릭(모바일: 더블탭)으로 배치 해제, 길게 누르면(0.5초) 단원 이름/메모 수정.
5. [PNG 다운로드]로 이미지 저장, 카카오톡/밴드 등에 공유.

## 주요 기능

- 드래그&드롭(PC) + 탭 선택(모바일) 배치
- 🎲 랜덤배치 — 누를 때마다 전체 단원을 좌석에 무작위 재배치
- 좌석 클릭 선택 → 다른 좌석으로 이동/서로 교체
- 한글 가나다 자동 정렬, 초성(ㄱ~ㅎ) 필터
- 좌·우 9행 레이아웃 직접 편집
- **보조석**: 단원 수가 본좌석을 초과하면 하단에 `보01~` 구역 자동 표시 (열 수 3~10 설정, 행 자동, 배치 UX 동일)
- 브라우저 localStorage 자동 저장
- PNG 이미지 저장 (보조석 포함, 콘텐츠 폭에 맞춰 여백 없이 캡처 → 미리보기 모달에서 파일 저장/공유/길게 눌러 저장)
- 완전 오프라인 동작 (html2canvas 파일 내 인라인, 단일 HTML)
- 📖 사용법 버튼 — 홈 화면 추가·좌석 배치/교체·카톡 공유 방법을 앱 안에서 바로 확인

## 데이터 위치

브라우저 localStorage 키: `seating-chart-v1`
시크릿 모드에서는 저장이 비활성화되며 상단에 경고가 표시됩니다.

## 자가테스트

`index.html?test=1`로 열면 콘솔에 자가테스트 결과가 출력됩니다.

## 수동 체크리스트

- [ ] 단원 추가/수정/삭제
- [ ] 가나다 정렬
- [ ] 초성 필터 ㄱ~ㅎ
- [ ] 드래그&드롭 (PC)
- [ ] 탭 선택 배치 (모바일)
- [ ] 🎲 랜덤배치 (누를 때마다 다른 배치, 좌석 수 초과 단원은 미배치 유지, 보조석 배치는 유지)
- [ ] 좌석 ↔ 좌석 스왑 (드래그)
- [ ] 점유 좌석 클릭 선택 → 빈 좌석 클릭 이동
- [ ] 점유 좌석 클릭 선택 → 점유 좌석 클릭 교체
- [ ] 좌석 → 명단 드래그 해제
- [ ] 더블클릭/더블탭 해제
- [ ] 길게 누르기(0.5초) → 단원 수정 모달
- [ ] 카운터 갱신
- [ ] Left 우측 정렬, Right 좌측 정렬
- [ ] 빈 행 통로 표시
- [ ] 레이아웃 편집 후 사라진 좌석 자동 복귀
- [ ] 보조석: 초과 인원만큼 칸 표시, 초과 없으면 미표시
- [ ] 보조석 배치/스왑/해제 (본좌석과 동일 UX)
- [ ] 보조석 열 수 변경 시 배치 유지 + 줄바꿈만 변경
- [ ] PNG 다운로드 → 미리보기 모달 표시, [파일로 저장] 동작 (PC + 모바일, 좌우 여백 없이)
- [ ] PNG 미리보기 모달 [공유] — 지원 기기에서 공유 시트 표시, 취소 시 오류 없음
- [ ] 인터넷 차단 상태에서 열어도 전 기능 동작
- [ ] 📖 사용법 버튼 → 매뉴얼 모달 표시, 스크롤·닫기 동작 (PC + 모바일)
- [ ] 초기화 (배치만 해제)
- [ ] 새로고침 후 데이터 유지
- [ ] 시크릿 모드 경고 배너

## 기술 스택

Vanilla JS + HTML5 DnD + localStorage + html2canvas 1.4.1 (파일 내 인라인). 빌드 도구 없음, 인터넷 연결 불필요.
